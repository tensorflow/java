// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: tensorflow/core/framework/graph.proto
// Protobuf Java Version: 4.28.3

package org.tensorflow.proto;

public interface GraphDefOrBuilder extends
    // @@protoc_insertion_point(interface_extends:tensorflow.GraphDef)
    com.google.protobuf.MessageOrBuilder {

  /**
   * <code>repeated .tensorflow.NodeDef node = 1;</code>
   */
  java.util.List<org.tensorflow.proto.NodeDef> 
      getNodeList();
  /**
   * <code>repeated .tensorflow.NodeDef node = 1;</code>
   */
  org.tensorflow.proto.NodeDef getNode(int index);
  /**
   * <code>repeated .tensorflow.NodeDef node = 1;</code>
   */
  int getNodeCount();
  /**
   * <code>repeated .tensorflow.NodeDef node = 1;</code>
   */
  java.util.List<? extends org.tensorflow.proto.NodeDefOrBuilder> 
      getNodeOrBuilderList();
  /**
   * <code>repeated .tensorflow.NodeDef node = 1;</code>
   */
  org.tensorflow.proto.NodeDefOrBuilder getNodeOrBuilder(
      int index);

  /**
   * <pre>
   * Compatibility versions of the graph.  See core/public/version.h for version
   * history.  The GraphDef version is distinct from the TensorFlow version, and
   * each release of TensorFlow will support a range of GraphDef versions.
   * </pre>
   *
   * <code>.tensorflow.VersionDef versions = 4;</code>
   * @return Whether the versions field is set.
   */
  boolean hasVersions();
  /**
   * <pre>
   * Compatibility versions of the graph.  See core/public/version.h for version
   * history.  The GraphDef version is distinct from the TensorFlow version, and
   * each release of TensorFlow will support a range of GraphDef versions.
   * </pre>
   *
   * <code>.tensorflow.VersionDef versions = 4;</code>
   * @return The versions.
   */
  org.tensorflow.proto.VersionDef getVersions();
  /**
   * <pre>
   * Compatibility versions of the graph.  See core/public/version.h for version
   * history.  The GraphDef version is distinct from the TensorFlow version, and
   * each release of TensorFlow will support a range of GraphDef versions.
   * </pre>
   *
   * <code>.tensorflow.VersionDef versions = 4;</code>
   */
  org.tensorflow.proto.VersionDefOrBuilder getVersionsOrBuilder();

  /**
   * <pre>
   * Deprecated single version field; use versions above instead.  Since all
   * GraphDef changes before "versions" was introduced were forward
   * compatible, this field is entirely ignored.
   * </pre>
   *
   * <code>int32 version = 3 [deprecated = true];</code>
   * @deprecated tensorflow.GraphDef.version is deprecated.
   *     See tensorflow/core/framework/graph.proto;l=27
   * @return The version.
   */
  @java.lang.Deprecated int getVersion();

  /**
   * <pre>
   * "library" provides user-defined functions.
   *
   * Naming:
   * * library.function.name are in a flat namespace.
   * NOTE: We may need to change it to be hierarchical to support
   * different orgs. E.g.,
   * { "/google/nn", { ... }},
   * { "/google/vision", { ... }}
   * { "/org_foo/module_bar", { ... }}
   * map&lt;string, FunctionDefLib&gt; named_lib;
   * * If node[i].op is the name of one function in "library",
   * node[i] is deemed as a function call. Otherwise, node[i].op
   * must be a primitive operation supported by the runtime.
   *
   *
   * Function call semantics:
   *
   * * The callee may start execution as soon as some of its inputs
   * are ready. The caller may want to use Tuple() mechanism to
   * ensure all inputs are ready in the same time.
   *
   * * The consumer of return values may start executing as soon as
   * the return values the consumer depends on are ready.  The
   * consumer may want to use Tuple() mechanism to ensure the
   * consumer does not start until all return values of the callee
   * function are ready.
   * </pre>
   *
   * <code>.tensorflow.FunctionDefLibrary library = 2;</code>
   * @return Whether the library field is set.
   */
  boolean hasLibrary();
  /**
   * <pre>
   * "library" provides user-defined functions.
   *
   * Naming:
   * * library.function.name are in a flat namespace.
   * NOTE: We may need to change it to be hierarchical to support
   * different orgs. E.g.,
   * { "/google/nn", { ... }},
   * { "/google/vision", { ... }}
   * { "/org_foo/module_bar", { ... }}
   * map&lt;string, FunctionDefLib&gt; named_lib;
   * * If node[i].op is the name of one function in "library",
   * node[i] is deemed as a function call. Otherwise, node[i].op
   * must be a primitive operation supported by the runtime.
   *
   *
   * Function call semantics:
   *
   * * The callee may start execution as soon as some of its inputs
   * are ready. The caller may want to use Tuple() mechanism to
   * ensure all inputs are ready in the same time.
   *
   * * The consumer of return values may start executing as soon as
   * the return values the consumer depends on are ready.  The
   * consumer may want to use Tuple() mechanism to ensure the
   * consumer does not start until all return values of the callee
   * function are ready.
   * </pre>
   *
   * <code>.tensorflow.FunctionDefLibrary library = 2;</code>
   * @return The library.
   */
  org.tensorflow.proto.FunctionDefLibrary getLibrary();
  /**
   * <pre>
   * "library" provides user-defined functions.
   *
   * Naming:
   * * library.function.name are in a flat namespace.
   * NOTE: We may need to change it to be hierarchical to support
   * different orgs. E.g.,
   * { "/google/nn", { ... }},
   * { "/google/vision", { ... }}
   * { "/org_foo/module_bar", { ... }}
   * map&lt;string, FunctionDefLib&gt; named_lib;
   * * If node[i].op is the name of one function in "library",
   * node[i] is deemed as a function call. Otherwise, node[i].op
   * must be a primitive operation supported by the runtime.
   *
   *
   * Function call semantics:
   *
   * * The callee may start execution as soon as some of its inputs
   * are ready. The caller may want to use Tuple() mechanism to
   * ensure all inputs are ready in the same time.
   *
   * * The consumer of return values may start executing as soon as
   * the return values the consumer depends on are ready.  The
   * consumer may want to use Tuple() mechanism to ensure the
   * consumer does not start until all return values of the callee
   * function are ready.
   * </pre>
   *
   * <code>.tensorflow.FunctionDefLibrary library = 2;</code>
   */
  org.tensorflow.proto.FunctionDefLibraryOrBuilder getLibraryOrBuilder();

  /**
   * <pre>
   * Stack traces for the nodes in this graph.
   * </pre>
   *
   * <code>.tensorflow.GraphDebugInfo debug_info = 5;</code>
   * @return Whether the debugInfo field is set.
   */
  boolean hasDebugInfo();
  /**
   * <pre>
   * Stack traces for the nodes in this graph.
   * </pre>
   *
   * <code>.tensorflow.GraphDebugInfo debug_info = 5;</code>
   * @return The debugInfo.
   */
  org.tensorflow.proto.GraphDebugInfo getDebugInfo();
  /**
   * <pre>
   * Stack traces for the nodes in this graph.
   * </pre>
   *
   * <code>.tensorflow.GraphDebugInfo debug_info = 5;</code>
   */
  org.tensorflow.proto.GraphDebugInfoOrBuilder getDebugInfoOrBuilder();
}
